const numbers = [12, 21, 38, 5, 45, 37, 6];

function average(array) {
 let count = 0;
 let total = 0;
 for (number of numbers){
    count ++
    total += number
 }
return total/count
}

const finalValue = average(numbers);
console.log(finalValue);